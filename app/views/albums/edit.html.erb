<header class="navbar navbar-light sticky-top bg-light flex-md-nowrap p-0 shadow">
  <%=link_to "Back", albums1_path(username: current_user.username)%>
    
    <a class="navbar-brand col-md-3 col-lg-2 me-0 px-3" href="/">Page d'accueil</a>
  <button class="navbar-toggler position-absolute d-md-none collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#sidebarMenu" aria-controls="sidebarMenu" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <ul class="navbar-nav px-3">
    <li class="nav-item text-nowrap">
      <a class="nav-link" href="#">Sign out</a>
    </li>
  </ul>
</header>


<div class="container-fluid">
  <div class="row">
    <nav id="sidebarMenu" class="col-md-3 col-lg-2 d-md-block bg-light sidebar collapse">
      <div class="position-sticky pt-3">


        <h6 class="sidebar-heading d-flex justify-content-between align-items-center px-3 mt-4 mb-1 text-muted">
          <span>Saved reports</span>
          <a class="link-secondary" href="#" aria-label="Add a new report">
            <span data-feather="plus-circle"></span>
          </a>
        </h6>
        <ul class="nav flex-column mb-2">
          <li class="nav-item">
            <a class="nav-link" href="#">
              <span data-feather="file-text"></span>
              <%= render 'formedit', album: @album %>

            </a>
          </li>
        </ul>
      </div>
    </nav>

    <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
       <h1>Add Photos To Album</h1>
       <%= form_with(model: @album, local: true,html:{class: "myalbumsphotos2",id: "myalbumsphotos2id"}) do |form| %>
       <div class="myalbumsphotos2hidden" hidden="true"></div>
<div class="row row-cols-4 row-cols-md-4 g-4 myphotoswrap ">

    <%@images.each do |image|%>
              <%=form.fields_for :images, image do |myimg|%>
 <%=myimg.hidden_field :myname%>

<div class="col" hidden='true'>
    <div class="card">
      <img src="<%=image.name%>" class="card-img-top" alt="...">
      <div class="card-body">
          <p class="card-text">
<%=myimg.text_area :description, value: image.description%>
              
          </p>      
      </div>
    </div>
  </div>
    <%end%>
    <%end%>

</div>
       <%end%>
    </main>
  </div>
</div>






