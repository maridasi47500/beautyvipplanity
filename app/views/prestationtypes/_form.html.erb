<%= form_with(model: prestationtype, local: true) do |form| %>
  <% if prestationtype.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(prestationtype.errors.count, "error") %> prohibited this prestationtype from being saved:</h2>

      <ul>
        <% prestationtype.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
