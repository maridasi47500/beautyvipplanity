<!DOCTYPE html><html id="facebook" class="_9dls" lang="fr" dir="ltr"><head>
<title>Beauty vip La Garde Hyères</title>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8">

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-gtEjrD/SeCtmISkJkNUaaKMoLD0//ElJ19smozuHV6z3Iehds+3Ulb9Bn9Plx0x4" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js" integrity="sha384-IQsoLXl5PILFhosVNubq5LC7Qb9DXgDA9i+tQ8Zj3iwWAwPtgFTxbJ8NT4GN1R8p" crossorigin="anonymous"></script>
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/js/bootstrap.min.js" integrity="sha384-Atwg2Pkwv9vp0ygtn1JAojH0nYbwNJLPhwyoVbhoPwBhjQPR5VtM2+xf0Uwh9KtT" crossorigin="anonymous"></script>

<style>
  .marg1 {
      float:right;
      top:0;
    margin: 0 20px 0px 20px  ;
}
body {font-family: Arial, Helvetica, sans-serif;}

/* The Modal (background) */
.modal {
  display: none; /* Hidden by default */
  position: fixed; /* Stay in place */
  z-index: 1; /* Sit on top */
  padding-top: 100px; /* Location of the box */
  left: 0;
  top: 0;
  width: 100%; /* Full width */
  height: 100%; /* Full height */
  overflow: auto; /* Enable scroll if needed */
  background-color: rgb(0,0,0); /* Fallback color */
  background-color: rgba(0,0,0,0.4); /* Black w/ opacity */
}

/* Modal Content */
.modal-content {
  background-color: #fefefe;
  margin: auto;
  padding: 20px;
  border: 1px solid #888;
  width: 80%;
}

/* The Close Button */
.close {
  color: #aaaaaa;
  float: right;
  font-size: 28px;
  font-weight: bold;
}
.mycontent {
    display:block;
    width:100%;
    font-size: 22px;
    height: 300px;
}
.mycontent-comment {
    //display:block;
    //width:100%;
    overflow: hidden;
    resize: none; 
    
}
h3.mytitle {
    float:left;
    text-align:center;
    width:100%;
    display:block;
}
.myitems {
    width:100%;
    display:block;
    word-break: break-all;
}
.close:hover,
.close:focus {
  color: #000;
  text-decoration: none;
  cursor: pointer;
}

.myelement:before {
    content: "\e080";
    font-family: 'Glyphicons Halflings';
    font-size: 9px;
    position: relative;
    margin-right: 10px;
    top: 3px;
    color: #ccc;
}
.btnpublication {
    display:block;
    width: 100%;
}
.btnreview {
    float:right;
    right:0;
}
.myamenity {
    padding: 0 3px 0 3px;
}
.mybtnreview {
    display:block;
    text-align:right;
    
}
.nav-item {
  padding: 0 30px 0 30px;
}
    
</style>
    </head><body class="_6s5d _71pn _-kb d-flex justify-content-center">
    <article class="my-3 w-50 " id="card">
      <ul class="bg-light bd-heading nav sticky-xl-top align-self-start mt-5 mb-3 mt-xl-0 mb-xl-2">
          <li class="nav-item"><h3 >Beauty VIP - Our Posts</h3></li>
          <li class="nav-item"><a class="d-flex align-items-center nav-link" href="<%=root_path%>">Home Page</a></li>
          <li class="nav-item"><p class=""><%=render current_user%></p></li>
          <li class="nav-item"><p class=""><%=link_to me_path, {class: "nav-link"} do%>Ma page<%end%></p></li>

          <li class="nav-item"><p class=""><%=link_to 'Sign out', signout_path, {class: "nav-link"}%></p></li>
          <li class="nav-item"><p class=""><%=notice%></p></li>

      </ul>
          <div class="card">
              
              <img class="card-img-top" width="100%" height="180" src="/assets/202105272225270200.png" >
              <div class="card-body">
                <h5 class="card-title">Beauty vip La Garde Hyères</h5>
                <p class="card-text">@83130BeautyVIP  · Salon de beauté</p>
                
              </div>
              <div class="card-footer bg-transparent">
<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link" href="<%=root_path%>">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%=services_path%>">Services</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%=reviews_path%>">Avis</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%=photos_path%>">Photos</a>
        </li>
      </ul>
    </div>
  </div>
</nav>                  
              </div>            </div>

                <div class="bd-example">

             <%=yield%>  
            </div>

     

    </article>
        <div id="myModal" class="modal">

  <!-- Modal content -->
  <div class="modal-content myformpost">
  </div>

</div>

<script src="https://code.jquery.com/jquery-3.6.0.js" integrity="sha256-H+K7U5CnXl1h5ywQfKtSj8PCmoN9aaq30gDh27Xc0jk=" crossorigin="anonymous"></script>
        <script>
          function navig(){
            var allels=document.getElementsByClassName('nav-link'),el;
            for (var i= 0;(i<allels.length-1);i++) {
  el = document.getElementsByClassName('nav-link')[i];
    if (new URL(el.href.split('?')[0]).pathname === window.location.pathname.split('?')[0]) {
      
      el.parentElement.classList.add('active');
      
    }
  };
  }
</script>


<script>
  $(function(){
    navig();
  });

</script>

<script>
// Get the modal
var modal = document.getElementById("myModal");

// Get the button that opens the modal

// Get the <span> element that closes the modal
var span = document.getElementsByClassName("close")[0];

// When the user clicks the button, open the modal 
function on(e) {
  modal.style.display = "block";
}

// When the user clicks on <span> (x), close the modal
function off(e) {
  modal.style.display = "none";
}

// When the user clicks anywhere outside of the modal, close it
window.onclick = function(event) {
  if (event.target == modal) {
    modal.style.display = "none";
  }
}
function expandTextarea(e) {
    e.target.addEventListener('keyup', function() {
        this.style.overflow = 'hidden';
        this.style.height = 0;
        this.style.height = this.scrollHeight + 'px';
    }, false);
}
        function pressed(e) {
            // Has the enter key been pressed?
            if ( (window.e ? e.keyCode : e.which) == 13) { 
                // If it has been so, manually submit the <form>
                //e.target.parentElement.submit();
                par=$('#'+e.target.parentElement.id);
                $.ajax({type:'post',url: '/posts/'+par.data('id')+'/createcomment?'+par.serialize(),complete:function(){
                    $.ajax({url:'/lastcomment',success: function(data){$('.mynew'+par.data('id'))[0].outerHTML=(data);}});
                    e.target.value = "";
                }});
                  return false;
            }
        }
        function pressedreview(e) {
            // Has the enter key been pressed?
            if ( (window.e ? e.keyCode : e.which) == 13) { 
                // If it has been so, manually submit the <form>
                //e.target.parentElement.submit();
                par=$('#'+e.target.parentElement.id);
                $.ajax({type:'post',url: '/reviews/'+par.data('id')+'/createcomment?'+par.serialize(),complete:function(){
                    $.ajax({url:'/lastcommentreview',success: function(data){$('.mynew'+par.data('id'))[0].outerHTML=(data);}});
                    e.target.value = "";
                }});
                  return false;
            }
        }
        function pressedupdate(e) {
            // Has the enter key been pressed?
            if ( (window.e ? e.keyCode : e.which) == 13) { 
                // If it has been so, manually submit the <form>
                //e.target.parentElement.submit();
                par=$('#'+e.target.parentElement.id);
                $.ajax({type:'post',url: '/posts/'+par.data('id')+'/updatecomment/'+par.data('commentid')+'?'+par.serialize(),complete:function(){
                    $.ajax({url:'/posts/'+par.data('id')+'/mycomment/'+par.data('commentid'),
                      success: function(data){$('.mynewcomment'+par.data('commentid'))[0].outerHTML=(data);}});
                    e.target.value = "";
                }});
                  return false;
            }
        }
        function pressedupdatereview(e) {
            // Has the enter key been pressed?
            if ( (window.e ? e.keyCode : e.which) == 13) { 
                // If it has been so, manually submit the <form>
                //e.target.parentElement.submit();
                par=$('#'+e.target.parentElement.id);
                $.ajax({type:'post',url: '/reviews/'+par.data('id')+'/updatecomment/'+par.data('commentid')+'?'+par.serialize(),complete:function(){
                    $.ajax({url:'/reviews/'+par.data('id')+'/mycomment/'+par.data('commentid'),
                      success: function(data){$('.mynewcomment'+par.data('commentid'))[0].outerHTML=(data);}});
                    e.target.value = "";
                }});
                  return false;
            }
        }

</script>

</body></html>


